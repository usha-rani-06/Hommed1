{
  "name": "hommed-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && .venv\\Scripts\\python run.py",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npx -y concurrently --kill-others -n \"backend,frontend\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:backend": "cd backend && .venv\\Scripts\\pip install -r requirements.txt",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm install && npm run install:frontend && npm run install:backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
